import { prisma } from "../lib/prisma-client.js";
import type { Request, Response, NextFunction } from "express";
import z from "zod";

export const profileCreateController = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
